import { createMuiTheme } from '@material-ui/core/styles';

export default createMuiTheme({
  palette: {
    primary: {
      main: '#707071',
    },
    secondary: {
      main: '#a6a9ab',
    },
    background: {
      default: '#ffffff',
    },
  },

  typography: {
    htmlFontSize: 10,
    fontSize: 16,
    fontFamily: 'Open Sans, Big Caslon, sans-serif',
  },
});
